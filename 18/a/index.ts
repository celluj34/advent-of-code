import input from "../input.js";

type MyType = {
  left: MyType | number;
  right: MyType | number;
};

const start = () => {
  const inputs = input.split("\n");

  // console.log(`The final result is ${maxHeight}`);
};

start();
